id: woki.hello
name: Hello
version: 0.1.0
apiVersion: 1
runtime:
  wasm: extension.wasm
permissions:
  - log
# Optional:
#  - events
# libraries:
#   - math
#   - ecs
# Extensions with the events permission activate on the first public application event.
# Contributed commands also activate their extension lazily.
# activation:
#   startup: true
#   tick: true
# contributes:
#   commands:
#     - id: woki.hello.say
#       title: Say Hello
#       category: Examples
$schema: https://schemas.woki.dev/extension.manifest/v1.schema.json
