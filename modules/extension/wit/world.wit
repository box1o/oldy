package woki:extension@1.0.0;

world extension {
  import logging;
  import paths;
  import storage;
  import config;
  import events;

  export lifecycle;
}

/// The world for a guest whose manifest contributes commands.
world extension-with-commands {
  import logging;
  import paths;
  import storage;
  import config;
  import events;

  export lifecycle;
  export commands;
}
